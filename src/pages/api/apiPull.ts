// import { NextApiRequest, NextApiResponse } from 'next'


// type Data = {
//     name: string
//   }

// export default (req: NextApiRequest, res: NextApiResponse) => {
//   res.status(200).json({ name: 'John Doe' })
// }


// const pgp = require('pg-promise')();

// // Get the values for these variables from configuration
// const user = 'Derek'
// const password = 'light444'
// const host = 'localhost'
// const port = 5434
// const database = 'stocks_CAN_db'







// const db = pgp(`postgres://${user}:${password}@${host}:${port}/${database}`)
// //Then you can await on it in an async function:

// const post = await db.one('SELECT * FROM stock_list WHERE id=$1', id);